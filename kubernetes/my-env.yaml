apiVersion: v1 
kind: Pod 
metadata: 
  name: my-app-pod 
  labels: 
    app: myapp
spec: 
   containers: 
      - name: nginx-container 
        image: nginx
        env:
          - name: APP_COLOR
            value: blue
          # valueFrom:
          #   configMapKeyRef:
                #  name: app-config
                #  key: APP_COLOR
          #   secretKeyRef: 